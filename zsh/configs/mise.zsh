# mise: unified runtime version manager (replaces nvm + chruby)
# Shims are on PATH via zshenv for non-interactive shells (scripts, nvim, etc.)
# activate adds cd-hook for auto-switching in interactive shells
eval "$(mise activate zsh)"
